import 'package:flutter/material.dart';

class uploadassign extends StatelessWidget {
  const uploadassign({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Social role in modern warfare'),
      ),
    );
  }
}