import 'package:flutter/material.dart';

class viewassignment extends StatelessWidget {
  const viewassignment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Social Role in warfare'),
      ),
      body:  Container(),
    );
  }
}